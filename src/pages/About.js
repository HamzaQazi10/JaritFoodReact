import React, { useContext } from "react";

const About = () => {
  const data = {
    name: "PackageX",
  };
  const { myName } = useProductContext();
  return (
    <>
      <h1>jj</h1>
    </>
  );
};

export default About;
